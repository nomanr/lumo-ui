name: Build and Verify

on:
  workflow_dispatch:
  pull_request:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build and Verify
        run: bash .github/scripts/clean-build-verify.sh
